{% extends 'base.html.twig' %}

{% block title %}KartaProduktu{% endblock %}

{% block body %}
    <h1>KartaProduktu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ karta_produktu.id }}</td>
            </tr>
            <tr>
                <th>NazwaProduktu</th>
                <td>{{ karta_produktu.nazwaProduktu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('karta_produktu_index') }}">back to list</a>

    <a href="{{ path('karta_produktu_edit', {'id': karta_produktu.id}) }}">edit</a>

    {{ include('karta_produktu/_delete_form.html.twig') }}
{% endblock %}
